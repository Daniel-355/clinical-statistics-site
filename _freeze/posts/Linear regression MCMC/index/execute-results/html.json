{
  "hash": "f09735b8c47c61fd8848195883d8e0d1",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Linear regression MCMC\"\ndescription: \"A practical R learning guide to Linear regression MCMC, with concise explanations, reproducible examples, and interpretation guidance.\"\ndate: 2026-08-31\nsection: \"Biostatistics\"\ncategory: \"Adaptive & Bayesian Methods\"\ntopic: \"Linear regression MCMC\"\ntype: article\ntags: [\"Adaptive & Bayesian Methods\", \"Biostatistics\", \"Linear\", \"regression\", \"MCMC\"]\nstatus: published\nfeatured: true\nyoutube: []\npdf: []\ndownloads: []\nvalidate-yaml: false\nexecute:\n  echo: true\n  warning: false\n  error: true\n---\n\n## Why This Topic Matters\n\nAdaptive and Bayesian methods update evidence as data accumulate and express uncertainty through probability distributions or decision rules.\n\n## Learning Objectives\n\nAfter working through this material, you should be able to:\n\n- explain the main ideas behind **Linear regression MCMC**;\n- interpret the R workflow and its output; and\n- adapt the examples to a related analysis.\n\n## Lesson Roadmap\n\nThe original material is organized around the following topics:\n\n1. Simple linear regression\n2. Metropolis-Hastings algorithm\n3. Plot parameters\n4. Linear regression MCMC in JAGS\n5. Plot(fitlm1)\n6. Prediction\n\n## How to Use This Lesson\n\nRead the explanation before each workflow, then run the R chunks in sequence. Code and output are shown together so that each numerical or graphical result can be connected to the command that produced it. Some source examples require optional packages or external data. Those setup-dependent chunks remain visible for reference but are marked `eval: false` when they cannot be reproduced safely in a clean website build.\n\n## Reproducible Learning Check\n\nThis short R chunk confirms the scope of the lesson and always produces a visible output.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlearning_overview <- data.frame(\n  section = \"Biostatistics\",\n  category = \"Adaptive & Bayesian Methods\",\n  topic = \"Linear regression MCMC\"\n)\nlearning_overview\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n        section                    category                  topic\n1 Biostatistics Adaptive & Bayesian Methods Linear regression MCMC\n```\n\n\n:::\n:::\n\n\n## Learning Material\n\nThe following material develops **Linear regression MCMC** from core ideas to practical implementation. Definitions and assumptions come first; worked code then shows how the ideas are represented computationally.\n\n### Conceptual Foundation\n\nThe material below closely follows the original R Markdown source, with its examples retained for study and reuse.\n\n\n\n#### Simple linear regression\n\n<!-- generated-learning-note -->\nThis section develops **Simple linear regression**. Read the model specification together with its assumptions and the scale of each reported effect.\n\n\n`makes the posterior to be near prior = MCMC stability`\n\n### R Workflow and Examples\n\nRun the examples in order because later blocks may reuse objects created earlier. Each block includes a short purpose statement and guidance for reading its output.\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block generates controlled example data. Simulation makes the data-generating assumptions explicit and provides a known setting in which to study the method.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Creating data\ntrueA = 5\ntrueB = 0\ntrueSd = 10\nsampleSize = 31\n# create independent x-values\nx =(-(sampleSize-1)/2):((sampleSize-1)/2)\n# create dependent values according to ax + b + N(0,sd)\ny = trueA * x + trueB + rnorm(n=sampleSize,mean=0,sd=trueSd)\nplot(x,y, main=\"Test Data\")\n```\n\n::: {.cell-output-display}\n![](index_files/figure-html/unnamed-chunk-1-1.png){width=672}\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Compare the generated sample with the parameters used to create it. Differences reflect random variation unless the code intentionally introduces bias or structure.\n\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block defines a reusable function. Separate its inputs, calculations, and returned value before applying it to new data.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# For estimating parameters in a Bayesian analysis, we need to derive the likelihood function for the model \nlikelihood = function(param){\n  a = param[1]\n  b = param[2]\n  sd = param[3]\n  \n  pred = a*x + b\n  singlelikelihoods = dnorm(y, mean = pred, sd = sd, log = T)\n  sumll = sum(singlelikelihoods)\n  return(sumll)\n}\n\n# Defining the prior\n# Prior distribution\nprior = function(param){\n  a = param[1]\n  b = param[2]\n  sd = param[3]\n  aprior = dunif(a, min=0, max=10, log = T)\n  bprior = dnorm(b, sd = 5, log = T)\n  sdprior = dunif(sd, min=0, max=30, log = T)\n  return(aprior+bprior+sdprior)\n}\n\n# The posterior\nposterior = function(param){\n  return (likelihood(param) + prior(param))\n}\n```\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Test the function with simple inputs whose expected answer is known before relying on it in a larger workflow.\n\n\n#### Metropolis-Hastings algorithm\n\n<!-- generated-learning-note -->\nThis subsection explains **Metropolis-Hastings algorithm** and shows how it contributes to the overall Linear regression MCMC workflow.\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block generates controlled example data. Simulation makes the data-generating assumptions explicit and provides a known setting in which to study the method.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# proposal function\nproposalfunction = function(param){\n  return(rnorm(3,mean = param, sd= c(0.1,0.5,0.3)))\n}\n\n# MH algorithm \nrun_metropolis_MCMC = function(startvalue, iterations){\n  chain = array(dim = c(iterations+1,3))\n  chain[1,] = startvalue\n  for (i in 1:iterations){\n    proposal = proposalfunction(chain[i,])\n    \n    probab = exp(posterior(proposal) - posterior(chain[i,]))     #p1/p2 = exp[log(p1)-log(p2)].\n    if (runif(1) < probab){   #get the uniform distribution points which meet the requirement / accept rate \n      chain[i+1,] = proposal\n    }else{\n      chain[i+1,] = chain[i,]\n    }\n  }\n  return(chain)\n}\n\n# run MH\nstartvalue = c(4,0,10)\nchain = run_metropolis_MCMC(startvalue, 10000)\n\n# acceptance rate\nburnIn = 5000\nacceptance = 1-mean(duplicated(chain[-(1:burnIn),]))  #The acceptance rate can be influenced by the proposal function\n```\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Compare the generated sample with the parameters used to create it. Differences reflect random variation unless the code intentionally introduces bias or structure.\n\n\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block carries out the next computational step in the workflow. Track the objects it creates and how they are used by later steps.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nacceptance\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 0.7936413\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Read the printed object, table, or scalar in the context of the preceding definitions. Confirm its units and expected range before continuing.\n\n#### Plot parameters\n\n<!-- generated-learning-note -->\nThis section uses **Plot parameters** to make patterns visible. Confirm what each axis, group, scale, and annotation represents.\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block creates a graphical summary. The plot should be read together with its axes, scales, groups, and any representation of uncertainty.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n### Summary: #######################\n\npar(mfrow = c(2,3))\nhist(chain[-(1:burnIn),1],nclass=30, , main=\"Posterior of a\", xlab=\"True value = red line\" )\nabline(v = mean(chain[-(1:burnIn),1]))\nabline(v = trueA, col=\"red\" )\nhist(chain[-(1:burnIn),2],nclass=30, main=\"Posterior of b\", xlab=\"True value = red line\")\nabline(v = mean(chain[-(1:burnIn),2]))\nabline(v = trueB, col=\"red\" )\nhist(chain[-(1:burnIn),3],nclass=30, main=\"Posterior of sd\", xlab=\"True value = red line\")\nabline(v = mean(chain[-(1:burnIn),3]) )\nabline(v = trueSd, col=\"red\" )\nplot(chain[-(1:burnIn),1], type = \"l\", xlab=\"True value = red line\" , main = \"Chain values of a\", )\nabline(h = trueA, col=\"red\" )\nplot(chain[-(1:burnIn),2], type = \"l\", xlab=\"True value = red line\" , main = \"Chain values of b\", )\nabline(h = trueB, col=\"red\" )\nplot(chain[-(1:burnIn),3], type = \"l\", xlab=\"True value = red line\" , main = \"Chain values of sd\", )\nabline(h = trueSd, col=\"red\" )\n```\n\n::: {.cell-output-display}\n![](index_files/figure-html/unnamed-chunk-5-1.png){width=672}\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Look for the intended pattern, but also inspect sparse groups, outliers, scale choices, and uncertainty before generalizing from the figure.\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block calculates descriptive summaries. Check the denominator, grouping variables, units, and treatment of missing values.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# slope\nmean(chain[-(1:burnIn),1])\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 4.746392\n```\n\n\n:::\n\n```{.r .cell-code}\n# intercept\nmean(chain[-(1:burnIn),2])\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 0.9536732\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Use the displayed values to understand center, spread, frequency, and missingness; investigate any implausible or unexpected result.\n\n\n\n#### Linear regression MCMC in JAGS\n\n<!-- generated-learning-note -->\nThis section develops **Linear regression MCMC in JAGS**. Read the model specification together with its assumptions and the scale of each reported effect.\n\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block carries out the next computational step in the workflow. Track the objects it creates and how they are used by later steps.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# create data\nsnakes1 <- data.frame(b_length = x, b_mass = y)\nhead(snakes1)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n  b_length    b_mass\n1      -15 -70.33117\n2      -14 -69.05556\n3      -13 -62.34025\n4      -12 -47.10840\n5      -11 -53.27932\n6      -10 -37.63815\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Read the printed object, table, or scalar in the context of the preceding definitions. Confirm its units and expected range before continuing.\n\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block loads the packages and supporting tools used by the following analysis. Package versions can affect numerical results and printed output.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(R2jags)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: package 'rjags' could not be loaded\n```\n\n\n:::\n\n```{.r .cell-code}\njagsdata_s1 <- with(snakes1, list(b_mass = b_mass, b_length = b_length, N = length(b_mass)))\n\nlm1_jags <- function(){\n  # Likelihood:\n  for (i in 1:N){\n    b_mass[i] ~ dnorm(mu[i], tau) # tau is precision (1 / variance)\n    mu[i] <- alpha + beta * b_length[i]\n  }\n  # Priors:\n  alpha ~ dnorm(0, 0.01) # intercept\n  beta ~ dnorm(0, 0.01) # slope\n  sigma ~ dunif(0, 100) # standard deviation\n  tau <- 1 / (sigma * sigma) # sigma^2 doesn't work in JAGS\n}\n\ninit_values <- function(){\n  list(alpha = rnorm(1), beta = rnorm(1), sigma = runif(1))\n}\n\nparams <- c(\"alpha\", \"beta\", \"sigma\")\n\nfit_lm1 <- jags(data = jagsdata_s1, inits = init_values, parameters.to.save = params, model.file = lm1_jags,\n                n.chains = 3, n.iter = 12000, n.burnin = 5000, n.thin = 10, DIC = F)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in jags(data = jagsdata_s1, inits = init_values, parameters.to.save = params, : could not find function \"jags\"\n```\n\n\n:::\n\n```{.r .cell-code}\nfit_lm1\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'fit_lm1' not found\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** A successful setup block is usually silent. Warnings about missing packages or version conflicts should be resolved before trusting later output.\n\n\n#### Plot(fitlm1)\n\n<!-- generated-learning-note -->\nThis section develops **Plot(fitlm1)**. Read the model specification together with its assumptions and the scale of each reported effect.\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block creates a graphical summary. The plot should be read together with its axes, scales, groups, and any representation of uncertainty.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# plot\n# plot(fit_lm1)\ntraceplot(fit_lm1, mfrow = c(2, 2), ask = F)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'fit_lm1' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nlm1_mcmc <- as.mcmc(fit_lm1)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'fit_lm1' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nplot(lm1_mcmc)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'lm1_mcmc' not found\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Look for the intended pattern, but also inspect sparse groups, outliers, scale choices, and uncertainty before generalizing from the figure.\n\n\n`If the Rhat values are substantially larger than one, the chains haven’t mixed properly and the posterior estimates cannot be trusted. `\n\n#### Prediction\n\n<!-- generated-learning-note -->\nThis subsection explains **Prediction** and shows how it contributes to the overall Linear regression MCMC workflow.\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block calculates descriptive summaries. Check the denominator, grouping variables, units, and treatment of missing values.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# new dataset\nnvalues <- 100\nb_length_new <- seq(min(snakes1$b_length), max(snakes1$b_length), length.out = nvalues)\n\n# we combine the three chains into one\nlm1_mcmc_combi <- as.mcmc(rbind(lm1_mcmc[[1]], lm1_mcmc[[2]], lm1_mcmc[[3]]))\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'lm1_mcmc' not found\n```\n\n\n:::\n\n```{.r .cell-code}\n# calculate the expected value of y for each of the new x\npred_mean_mean <- mean(lm1_mcmc_combi[, \"alpha\"]) + b_length_new * mean(lm1_mcmc_combi[, \"beta\"])\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'lm1_mcmc_combi' not found\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Use the displayed values to understand center, spread, frequency, and missingness; investigate any implausible or unexpected result.\n\n\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block carries out the next computational step in the workflow. Track the objects it creates and how they are used by later steps.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nhead(pred_mean_mean)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'pred_mean_mean' not found\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Read the printed object, table, or scalar in the context of the preceding definitions. Confirm its units and expected range before continuing.\n\n\n#### A kind of CI\n\n<!-- generated-learning-note -->\nThis subsection explains **A kind of CI** and shows how it contributes to the overall Linear regression MCMC workflow.\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block carries out the next computational step in the workflow. Track the objects it creates and how they are used by later steps.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# the uncertainty around this mean. \n# ci\npred_mean_dist <- matrix(NA, nrow = nrow(lm1_mcmc_combi), ncol = nvalues)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'lm1_mcmc_combi' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nfor (i in 1:nrow(pred_mean_dist)){\n  pred_mean_dist[i,] <- lm1_mcmc_combi[i,\"alpha\"] + b_length_new * lm1_mcmc_combi[i,\"beta\"]\n}\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'pred_mean_dist' not found\n```\n\n\n:::\n\n```{.r .cell-code}\ncredible_lower <- apply(pred_mean_dist, MARGIN = 2, quantile, prob = 0.025)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'pred_mean_dist' not found\n```\n\n\n:::\n\n```{.r .cell-code}\ncredible_upper <- apply(pred_mean_dist, MARGIN = 2, quantile, prob = 0.975)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'pred_mean_dist' not found\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Read the printed object, table, or scalar in the context of the preceding definitions. Confirm its units and expected range before continuing.\n\n\n#### Percentiles\n\n<!-- generated-learning-note -->\nThis subsection explains **Percentiles** and shows how it contributes to the overall Linear regression MCMC workflow.\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block generates controlled example data. Simulation makes the data-generating assumptions explicit and provides a known setting in which to study the method.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# percentile\nlm1_mcmc_combi_rep <- do.call(rbind, rep(list(lm1_mcmc_combi), 50)) # replication\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'lm1_mcmc_combi' not found\n```\n\n\n:::\n\n```{.r .cell-code}\n# Draw random values for all parameter combinations (rows) and body length values (columns):\npred_data_dist <- matrix(NA, nrow = nrow(lm1_mcmc_combi_rep), ncol = nvalues)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'lm1_mcmc_combi_rep' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nfor (i in 1:nrow(pred_data_dist)){\n  pred_data_dist[i,] <- lm1_mcmc_combi_rep[i,\"alpha\"] + b_length_new * lm1_mcmc_combi_rep[i,\"beta\"] +\n    rnorm(nvalues, mean = 0, sd = lm1_mcmc_combi_rep[i, \"sigma\"])\n}\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'pred_data_dist' not found\n```\n\n\n:::\n\n```{.r .cell-code}\n# Calculate quantiles:\nuncertain_lower <- apply(pred_data_dist, MARGIN = 2, quantile, prob = 0.025)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'pred_data_dist' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nuncertain_upper <- apply(pred_data_dist, MARGIN = 2, quantile, prob = 0.975)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'pred_data_dist' not found\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Compare the generated sample with the parameters used to create it. Differences reflect random variation unless the code intentionally introduces bias or structure.\n\n\n\n#### Plot CI and percentiles\n\n<!-- generated-learning-note -->\nThis section uses **Plot CI and percentiles** to make patterns visible. Confirm what each axis, group, scale, and annotation represents.\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block creates a graphical summary. The plot should be read together with its axes, scales, groups, and any representation of uncertainty.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# plot\npar(mfrow = c(1, 1))\nplot(b_mass ~ b_length, data = snakes1)\n```\n\n::: {.cell-output-display}\n![](index_files/figure-html/unnamed-chunk-14-1.png){width=672}\n:::\n\n```{.r .cell-code}\nlines(b_length_new, pred_mean_mean)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'pred_mean_mean' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nlines(b_length_new, credible_lower, lty = 2)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'credible_lower' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nlines(b_length_new, credible_upper, lty = 2)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'credible_upper' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nlines(b_length_new, uncertain_lower, lty = 2, col = \"red\")\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'uncertain_lower' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nlines(b_length_new, uncertain_upper, lty = 2, col = \"red\")\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'uncertain_upper' not found\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Look for the intended pattern, but also inspect sparse groups, outliers, scale choices, and uncertainty before generalizing from the figure.\n\n\nComparison with SLR:\n\n<!-- generated-learning-note -->\n**Purpose.** This block specifies and fits the statistical model. Identify the outcome, predictors, reference levels, link or distribution, and any correlation structure before interpreting coefficients.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(lm(snakes1$b_mass~snakes1$b_length))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n\nCall:\nlm(formula = snakes1$b_mass ~ snakes1$b_length)\n\nResiduals:\n     Min       1Q   Median       3Q      Max \n-20.6555  -4.6233   0.4826   6.5156  15.9053 \n\nCoefficients:\n                 Estimate Std. Error t value Pr(>|t|)    \n(Intercept)        0.9321     1.7609   0.529    0.601    \nsnakes1$b_length   4.7831     0.1969  24.295   <2e-16 ***\n---\nSignif. codes:  0 '***' 0.001 '**' 0.01 '*' 0.05 '.' 0.1 ' ' 1\n\nResidual standard error: 9.804 on 29 degrees of freedom\nMultiple R-squared:  0.9532,\tAdjusted R-squared:  0.9516 \nF-statistic: 590.3 on 1 and 29 DF,  p-value: < 2.2e-16\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Focus on coefficient direction and scale, uncertainty intervals, fit statistics, and diagnostics. Statistical significance alone does not establish practical importance or model adequacy.\n\n\n\n\n**The above three methonds have similar results. **\n\n#### Multiple regression in JAGS\n\n<!-- generated-learning-note -->\nThis section develops **Multiple regression in JAGS**. Read the model specification together with its assumptions and the scale of each reported effect.\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block carries out the next computational step in the workflow. Track the objects it creates and how they are used by later steps.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# multiple regression \n# create dataset\nsnakes2 <- data.frame(b_length = x, b_mass = y, sex = rep(c(0,1),times=16)[1:31] )\nhead(snakes2)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n  b_length    b_mass sex\n1      -15 -70.33117   0\n2      -14 -69.05556   1\n3      -13 -62.34025   0\n4      -12 -47.10840   1\n5      -11 -53.27932   0\n6      -10 -37.63815   1\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Read the printed object, table, or scalar in the context of the preceding definitions. Confirm its units and expected range before continuing.\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block specifies and fits the statistical model. Identify the outcome, predictors, reference levels, link or distribution, and any correlation structure before interpreting coefficients.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(lm( snakes2$b_mass~snakes2$b_length +snakes2$sex,data=snakes2))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n\nCall:\nlm(formula = snakes2$b_mass ~ snakes2$b_length + snakes2$sex, \n    data = snakes2)\n\nResiduals:\n    Min      1Q  Median      3Q     Max \n-19.946  -5.380   1.192   7.225  16.615 \n\nCoefficients:\n                 Estimate Std. Error t value Pr(>|t|)    \n(Intercept)        0.2228     2.4870    0.09    0.929    \nsnakes2$b_length   4.7831     0.1998   23.94   <2e-16 ***\nsnakes2$sex        1.4658     3.5752    0.41    0.685    \n---\nSignif. codes:  0 '***' 0.001 '**' 0.01 '*' 0.05 '.' 0.1 ' ' 1\n\nResidual standard error: 9.948 on 28 degrees of freedom\nMultiple R-squared:  0.9534,\tAdjusted R-squared:  0.9501 \nF-statistic: 286.7 on 2 and 28 DF,  p-value: < 2.2e-16\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Focus on coefficient direction and scale, uncertainty intervals, fit statistics, and diagnostics. Statistical significance alone does not establish practical importance or model adequacy.\n\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block creates a graphical summary. The plot should be read together with its axes, scales, groups, and any representation of uncertainty.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(b_mass ~ b_length, col = (sex + 1), data = snakes2)\n```\n\n::: {.cell-output-display}\n![](index_files/figure-html/unnamed-chunk-18-1.png){width=672}\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Look for the intended pattern, but also inspect sparse groups, outliers, scale choices, and uncertainty before generalizing from the figure.\n\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block generates controlled example data. Simulation makes the data-generating assumptions explicit and provides a known setting in which to study the method.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# format the data\njagsdata_s2 <- with(snakes2, list(b_mass = b_mass, b_length = b_length, sex = sex, N = length(b_mass)))\n\n# an R function containing the JAGS code:\n  lm2_jags <- function(){\n    # Likelihood:\n    for (i in 1:N){\n      b_mass[i] ~ dnorm(mu[i], tau) # tau is precision (1 / variance)\n      mu[i] <- alpha[1]   + beta[1] * sex[i] + beta[2] * b_length[i]\n    }\n    # Priors:\n    alpha[1] ~ dnorm(0, 0.01)\n    for (i in 1:2){\n      \n      beta[i] ~ dnorm(0, 0.01)\n    }\n    sigma ~ dunif(0, 100)\n    tau <- 1 / (sigma * sigma)\n  }\n\n  \n  # Markov chains and initial values\n  init_values <- function(){\n    list(alpha = rnorm(1), beta = rnorm(2), sigma = runif(1))\n  }\n  \n  params <- c(\"alpha\", \"beta\", \"sigma\")\n  \n  fit_lm2 <- jags(data = jagsdata_s2, inits = init_values, parameters.to.save = params, model.file = lm2_jags,\n                  n.chains = 3, n.iter = 12000, n.burnin = 2000, n.thin = 10, DIC = F)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in jags(data = jagsdata_s2, inits = init_values, parameters.to.save = params, : could not find function \"jags\"\n```\n\n\n:::\n\n```{.r .cell-code}\n  fit_lm2\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'fit_lm2' not found\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Compare the generated sample with the parameters used to create it. Differences reflect random variation unless the code intentionally introduces bias or structure.\n\n**Results are similar to the multiple regression**\n\nReferences\n\n[1](https://biometry.github.io/APES//LectureNotes/StatsCafe/Linear_models_jags.html)\n\n[2](https://theoreticalecology.wordpress.com/2010/09/17/metropolis-hastings-mcmc-in-r/)\n\n[3](https://pubmed.ncbi.nlm.nih.gov/31777998/)\n\n[4]()\n\n## Interpreting the Results\n\nInspect priors, likelihoods, posterior summaries, convergence, simulations, and operating characteristics. Treat the output as evidence generated under the stated data, coding choices, and assumptions. Before using the workflow in practice, replace example inputs with validated data and confirm that the method targets the intended scientific question.\n\n## Key Takeaways\n\n- **Connect method to purpose.** Identify the question and target quantity before choosing code or a model.\n- **Read code and output together.** Confirm what each object, estimate, interval, table, or plot represents.\n- **Check assumptions and diagnostics.** A successful computation is not by itself evidence that the result is reliable.\n- **Adapt deliberately.** When applying the example elsewhere, document changes to data definitions, parameters, and analytical choices.\n",
    "supporting": [
      "index_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}