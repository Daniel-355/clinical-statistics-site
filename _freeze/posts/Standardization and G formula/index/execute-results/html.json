{
  "hash": "d8888fa5266a4ea7998efebead96457f",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Standardization and G estimation\"\ndescription: \"A practical R learning guide to Standardization and G estimation, with concise explanations, reproducible examples, and interpretation guidance.\"\ndate: 2026-08-31\nsection: \"Epidemiology & Real-World Evidence\"\ncategory: \"Causal Methods\"\ntopic: \"Standardization and G estimation\"\ntype: article\ntags: [\"Causal Methods\", \"Epidemiology & Real-World Evidence\", \"Standardization\", \"G\", \"estimation\"]\nstatus: published\nfeatured: true\nyoutube: []\npdf: []\ndownloads: []\nvalidate-yaml: false\nexecute:\n  echo: true\n  warning: false\n  error: true\n---\n\n## Why This Topic Matters\n\nCausal methods combine design assumptions and statistical adjustment to estimate well-defined treatment or exposure contrasts.\n\n## Learning Objectives\n\nAfter working through this material, you should be able to:\n\n- explain the main ideas behind **Standardization and G estimation**;\n- interpret the R workflow and its output; and\n- adapt the examples to a related analysis.\n\n## Lesson Roadmap\n\nThe original material is organized around the following topics:\n\n1. Standardization and G estimation\n\n## How to Use This Lesson\n\nRead the explanation before each workflow, then run the R chunks in sequence. Code and output are shown together so that each numerical or graphical result can be connected to the command that produced it. Some source examples require optional packages or external data. Those setup-dependent chunks remain visible for reference but are marked `eval: false` when they cannot be reproduced safely in a clean website build.\n\n## Reproducible Learning Check\n\nThis short R chunk confirms the scope of the lesson and always produces a visible output.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlearning_overview <- data.frame(\n  section = \"Epidemiology & Real-World Evidence\",\n  category = \"Causal Methods\",\n  topic = \"Standardization and G estimation\"\n)\nlearning_overview\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n                             section       category\n1 Epidemiology & Real-World Evidence Causal Methods\n                             topic\n1 Standardization and G estimation\n```\n\n\n:::\n:::\n\n\n## Learning Material\n\nThe following material develops **Standardization and G estimation** from core ideas to practical implementation. Definitions and assumptions come first; worked code then shows how the ideas are represented computationally.\n\n### Conceptual Foundation\n\nThe material below closely follows the original R Markdown source, with its examples retained for study and reuse.\n\n\n\n#### Standardization and G estimation\n\n<!-- generated-learning-note -->\nThis subsection explains **Standardization and G estimation** and shows how it contributes to the overall Standardization and G estimation workflow.\n\n\nThe standardized mean (typical weighting on the outcome) is the weighted average of the conditional means in each stratum, and the weights are the probability of occurrence Pr [L = l ] in each stratum. But in high-dimensional data—such as our smoking cessation example—it is impossible for us to estimate E[Y|A=1,C =0,L=l] in a nonparametric way (C=0 means no censor).  \n\nThat is, when calculating the standardized mean, the stratum with the largest number of people has the most weight. \n\n- Estimated prediction mean by model\n\n### R Workflow and Examples\n\nRun the examples in order because later blocks may reuse objects created earlier. Each block includes a short purpose statement and guidance for reading its output.\n\n\n<!-- generated-learning-note -->\n**Purpose.** This source block depends on optional software, external data, or a machine-specific path. It is retained as a reference and is not executed during the website build.\n\n\n::: {.cell}\n\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Because this block is not executed, it produces no website output. Enable it only after its dependencies and input paths have been validated.\n\n\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block specifies and fits the statistical model. Identify the outcome, predictors, reference levels, link or distribution, and any correlation structure before interpreting coefficients.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# some preprocessing of the data\nnhefs$cens <- ifelse(is.na(nhefs$wt82), 1, 0)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'nhefs' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nfit <- glm(wt82_71 ~ qsmk + sex + race + age + I(age*age) + as.factor(education)\n           + smokeintensity + I(smokeintensity*smokeintensity) + smokeyrs\n           + I(smokeyrs*smokeyrs) + as.factor(exercise) + as.factor(active)\n           + wt71 + I(wt71*wt71) + qsmk*smokeintensity, data=nhefs)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in eval(mf, parent.frame()): object 'nhefs' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nsummary(fit)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'fit' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nnhefs$predicted.meanY <- predict(fit, nhefs) # conditional E[Y | A=1,C =0,L= l] \n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'fit' not found\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Focus on coefficient direction and scale, uncertainty intervals, fit statistics, and diagnostics. Statistical significance alone does not establish practical importance or model adequacy.\n\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block carries out the next computational step in the workflow. Track the objects it creates and how they are used by later steps.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nnhefs[which(nhefs$seqn==24770), c(\"predicted.meanY\", \"qsmk\", \"sex\", \n                                 \"race\", \"age\", \"education\", \n                                 \"smokeintensity\", \"smokeyrs\", \"exercise\",\n                                 \"active\", \"wt71\")]\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'nhefs' not found\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Read the printed object, table, or scalar in the context of the preceding definitions. Confirm its units and expected range before continuing.\n\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block extracts inferential or predictive results from fitted objects. Match each estimate, interval, contrast, or diagnostic to the quantity it represents.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(nhefs$predicted.meanY[nhefs$cens==0])\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'nhefs' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nsummary(nhefs$wt82_71[nhefs$cens==0])\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'nhefs' not found\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Interpret estimates on the scale reported by the function, and use intervals or other uncertainty measures when drawing conclusions.\n\n\nOur goal is to estimate when A=a, ∑E[Y|A=a,C=0,L =l]*Pr[L l]. The summation in L should be written as integral notation, since the variables in L are essentially continuous and cannot be represented by simple probabilities. But whatever the notation, we have now estimated E [Y | A = a, C =0, L = l ] for all combinations of A and L. Next we need to normalize these means according to the value l in L.\n\n\n\n- Standardization of outcome means according to the distribution of confounding variables\n\nIn principle, we could compute this probability non parametrically by simply dividing the number of people with L = l by the total number of people.  \n\nNext we describe how to estimate in the treatment group ( A = 1 ) and the non-treatment group ( A = 0 ). \n\n\nThis method has four steps: data augmentation (create a data matrix), outcome regression, prediction, and standardization (`also can use the linear regression formula`).\n\n\nWe need to add the second and third plates to the original data, fit the model  of E[Y|A=a,C =0,L= l], and then generate the predicted value. The mean of the predicted values in the second panel is 1.66, the value in the third panel is 5.18, and the causal effect E(Ya1,c0)-E(Ya0,c0) is 3.5.\n\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block specifies and fits the statistical model. Identify the outcome, predictors, reference levels, link or distribution, and any correlation structure before interpreting coefficients.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# create a dataset with 3 copies of each subject\nnhefs$interv <- -1 # 1st copy: equal to original one\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'nhefs' not found\n```\n\n\n:::\n\n```{.r .cell-code}\n# original one\n\ninterv0 <- nhefs # 2nd copy: treatment set to 0, outcome to missing \n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'nhefs' not found\n```\n\n\n:::\n\n```{.r .cell-code}\ninterv0$interv <- 0 # a=0\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'interv0' not found\n```\n\n\n:::\n\n```{.r .cell-code}\ninterv0$qsmk <- 0 #  \n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'interv0' not found\n```\n\n\n:::\n\n```{.r .cell-code}\ninterv0$wt82_71 <- NA\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'interv0' not found\n```\n\n\n:::\n\n```{.r .cell-code}\ninterv1 <- nhefs # 3rd copy: treatment set to 1, outcome to missing\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'nhefs' not found\n```\n\n\n:::\n\n```{.r .cell-code}\ninterv1$interv <- 1 #  \n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'interv1' not found\n```\n\n\n:::\n\n```{.r .cell-code}\ninterv1$qsmk <- 1 # A=1\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'interv1' not found\n```\n\n\n:::\n\n```{.r .cell-code}\ninterv1$wt82_71 <- NA #\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'interv1' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nonesample <- rbind(nhefs, interv0, interv1) # combining datasets # by rows\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'nhefs' not found\n```\n\n\n:::\n\n```{.r .cell-code}\n# linear model to estimate mean outcome conditional on treatment and confounders . parameters are estimated using original observations only (nhefs)\n\nstd <- glm(wt82_71 ~ qsmk + sex + race + age + I(age*age)\n           + as.factor(education) + smokeintensity \n           + I(smokeintensity*smokeintensity) + smokeyrs \n           + I(smokeyrs*smokeyrs) + as.factor(exercise) \n           + as.factor(active) + wt71 + I(wt71*wt71) + I(qsmk*smokeintensity), \n           data=onesample)# modeling\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in eval(mf, parent.frame()): object 'onesample' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nsummary(std)   \n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'std' not found\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Focus on coefficient direction and scale, uncertainty intervals, fit statistics, and diagnostics. Statistical significance alone does not establish practical importance or model adequacy.\n\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block extracts inferential or predictive results from fitted objects. Match each estimate, interval, contrast, or diagnostic to the quantity it represents.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nonesample$predicted_meanY <- predict(std, onesample) # prediction\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'std' not found\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Interpret estimates on the scale reported by the function, and use intervals or other uncertainty measures when drawing conclusions.\n\n\n\n\n<!-- generated-learning-note -->\n**Purpose.** This block calculates descriptive summaries. Check the denominator, grouping variables, units, and treatment of missing values.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# estimate mean outcome in each of the groups interv=0, and interv=1\n# this mean outcome is a weighted average of the mean outcomes in each combination of values of treatment and confounders, that is, the standardized outcome\nmean(onesample[which(onesample$interv==-1),]$predicted_meanY) # calculate\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'onesample' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nmean(onesample[which(onesample$interv==0),]$predicted_meanY)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'onesample' not found\n```\n\n\n:::\n\n```{.r .cell-code}\nmean(onesample[which(onesample$interv==1),]$predicted_meanY)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError: object 'onesample' not found\n```\n\n\n:::\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Use the displayed values to understand center, spread, frequency, and missingness; investigate any implausible or unexpected result.\n\n\n- Bootstrap method to calculate the confidence interval\n\n<!-- generated-learning-note -->\n**Purpose.** This source block depends on optional software, external data, or a machine-specific path. It is retained as a reference and is not executed during the website build.\n\n\n::: {.cell}\n\n```{.r .cell-code  code-fold=\"true\"}\n#install.packages(\"boot\") # install package if required\nlibrary(boot) # \n\n# function to calculate difference in means\n# indices -i by row，frequencies-f ，weights-w   ，how to smaple\n\nstandardization <- function(data, indices) {\n  # create a dataset with 3 copies of each subject\n  d <- data[indices,] # 1st copy: equal to original one，\n  d$interv <- -1\n  d0 <- d # 2nd copy: treatment set to 0, outcome to missing\n  d0$interv <- 0\n  d0$qsmk <- 0\n  d0$wt82_71 <- NA\n  d1 <- d # 3rd copy: treatment set to 1, outcome to missing\n  d1$interv <- 1\n  d1$qsmk <- 1\n  d1$wt82_71 <- NA\n  d.onesample <- rbind(d, d0, d1) # combining datasets\n  \n  # parameter estimates are used to predict mean outcome for observations with set treatment (interv=0 and interv=1)\n  fit <- glm(wt82_71 ~ qsmk + sex + race + age + I(age*age) + \n               as.factor(education) + smokeintensity + \n               I(smokeintensity*smokeintensity) + smokeyrs + I(smokeyrs*smokeyrs) +\n               as.factor(exercise) + as.factor(active) + wt71 + I(wt71*wt71), \n             data=d.onesample)\n  \n  d.onesample$predicted_meanY <- predict(fit, d.onesample)\n  \n  # estimate mean outcome in each of the groups interv=-1, interv=0, and interv=1\n  return(c(mean(d.onesample$predicted_meanY[d.onesample$interv==-1]),\n           mean(d.onesample$predicted_meanY[d.onesample$interv==0]),\n           mean(d.onesample$predicted_meanY[d.onesample$interv==1]),\n           mean(d.onesample$predicted_meanY[d.onesample$interv==1])-\n             mean(d.onesample$predicted_meanY[d.onesample$interv==0])))\n}\n\n# bootstrap\nresults <- boot(data=nhefs, statistic=standardization, R=5)\n\n# generating confidence intervals\nse <- c(sd(results$t[,1]), sd(results$t[,2]), \n        sd(results$t[,3]), sd(results$t[,4]))\nmean <- results$t0 # t0 ?\nll <- mean - qnorm(0.975)*se\nul <- mean + qnorm(0.975)*se\n\nbootstrap <- data.frame(cbind(c(\"Observed\", \"No Treatment\", \"Treatment\", \n                                \"Treatment - No Treatment\"), mean, se, ll, ul))\nbootstrap\n```\n:::\n\n\n<!-- generated-learning-note -->\n**How to read the output.** Because this block is not executed, it produces no website output. Enable it only after its dependencies and input paths have been validated.\n\n`ipw and standardization is suitbale for simple randomization control trial.`\n\nThe inverse probability weighted mean and the standardized mean are equal only when the model is not used, otherwise they are not necessarily equal. \n\nThe validity of causal inference depends on the following conditions: interchangeability, positivity, and consistency. It does not involve measurement errors and model errors.\n\nHowever, effect estimates from observational studies are always severely flawed. The problem is that no one can guarantee that these conditions will be perfectly established. Unmeasured confounding variables, nonoverlapping confounding variable distributions, poorly specified interventions, mismeasured variables, and misspecified models are all common problems we encounter in effect estimation. For example, we can try different models, but it is impossible to adjust for variables that are not measured. \n\nThe important problem with causal inference is that we cannot observe all the counterfactual data, so we use the above assumptions to approximate these data. The more we deviate from these assumptions, the more our effect estimates deviate from the true causal effect. Therefore, causal inferences drawn from observational studies must be viewed with caution. \n\n\nInverse probability weighting, standadization, and G-estimation are often collectively referred to as G-formula.\n\n## Interpreting the Results\n\nCheck nuisance models, overlap, weights, balance, diagnostics, estimand alignment, and sensitivity to modeling choices. Treat the output as evidence generated under the stated data, coding choices, and assumptions. Before using the workflow in practice, replace example inputs with validated data and confirm that the method targets the intended scientific question.\n\n## Key Takeaways\n\n- **Connect method to purpose.** Identify the question and target quantity before choosing code or a model.\n- **Read code and output together.** Confirm what each object, estimate, interval, table, or plot represents.\n- **Check assumptions and diagnostics.** A successful computation is not by itself evidence that the result is reliable.\n- **Adapt deliberately.** When applying the example elsewhere, document changes to data definitions, parameters, and analytical choices.\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}